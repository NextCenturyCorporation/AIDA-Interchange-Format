#!/bin/bash

docker run --rm -it \
        -e S3_SUBMISSION_BUCKET='' \
        -e S3_SUBMISSION_RESULTS_DIRECTORY='' \
        -e BATCH_INIT_DOCKER='' \
        -e BATCH_NUM_NODES='' \
        -e BATCH_JOB_DEFINITION='' \
        -e BATCH_JOB_QUEUE='' \
        -e AWS_SNS_TOPIC_ARN='' \
        -e AWS_DEFAULT_REGION='' \
        --name -batch-init-scheduler \
        batch-init-scheduler
