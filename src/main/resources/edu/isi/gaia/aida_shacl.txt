Here is a list of all the classes and properties in AidaAnnotationOntology.kt.

If it is indented, then it is in the InterchangeOntology.  If it has a dash before it, then
it was not part of InterchangeOntology, and it has been added.

Classes
----------
    Subgraph
    System
    Entity
    Event
    Relation
    Confidence
    TextJustification
    BoundingBox
    ImageJustification
    AudioJustification
-- changed KeyFrameVideoJustification
    ShotVideoJustification
-- added CompoundJustification
    LinkAssertion
    SameAsCluster
-- added ClusterMembership
    MutualExclusion
    MutualExclusionAlternative
    Hypothesis
    PrivateData

Properties
--------------------
-- system
-- hasName
-- textValue
-- numericValue
    confidence
    confidenceValue
-- justifiedBy
containedJustification
source
startOffset
startTimestamp
endTimestamp
boundingBox
boundingBoxUpperLeftX
boundingBoxUpperLeftY
boundingBoxLowerRightX
boundingBoxLowerRightY
keyFrame
shot
link
linkTarget
    prototype
    cluster
    clusterMember     <-- should this be member? (in InterchangeOntology)
    subgraphContains
    alternative
    alternativeGraph   <-- Missing from InterchangeOntology
-- noneOfTheAbove
-- dependsOnHypothesis
-- hypothesisContent
-- privateData
-- jsonContent

----------------------------------------------------------------------

Here are all the aida: references in the shacl file
In Shacl

     aida:alternative
     aida:alternativeGraph
     aida:AudioJustification
aida:boundingBox
     aida:BoundingBox
aida:boundingBoxLowerRightX
aida:boundingBoxLowerRightY
aida:boundingBoxUpperLeftX
aida:boundingBoxUpperLeftY
     aida:cluster
     aida:ClusterMember
     aida:CompoundJustification
     aida:confidence
     aida:Confidence
     aida:confidenceValue
aida:containedJustification
aida:dependsOnHypothesis
aida:endOffsetInclusive
aida:endTimestamp
     aida:Entity
aida:EntitySubclass
     aida:Event
aida:EventArgumentSubclass
aida:EventSubclass
aida:hasName
     aida:Hypothesis
aida:hypothesisContent
     aida:ImageJustification
aida:jsonContent
aida:justifiedBy
aida:keyFrame
aida:KeyFrameVideoJustification
aida:link
aida:LinkAssertion
aida:linkTarget
     aida:member
     aida:MutualExclusion
     aida:MutualExclusionAlternative
aida:noneOfTheAbove
aida:notthere
aida:numericValue
aida:privateData
     aida:PrivateData
     aida:prototype
     aida:Relation
aida:RelationArgumentSubclass
aida:RelationSubclass
     aida:SameAsCluster
aida:shot
     aida:ShotVideoJustification
aida:source
aida:startOffset
aida:startTimestamp
     aida:Subgraph
     aida:subgraphContains
aida:system
     aida:System
     aida:TextJustification
aida:textValue

Need to add ?
ClusterMembership
